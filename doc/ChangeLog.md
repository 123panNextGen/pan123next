- 新增：传输页支持「添加新下载」对话框，可粘贴外链直接下载到本地。
- 新增：下载器分片状态持久化，应用重启 / 崩溃后可继续断点续传。
- 新增：下载 URL 过期（401/403/410）自动刷新 123pan 链接并继续。
- 新增：单分片失败指数退避重试（1s / 2s / 4s），整体任务不再因单片失败而失败。
- 新增：续传前通过 HEAD 校验远端文件大小与 ETag，避免拼接到已变更的文件。
- 优化：传输页过滤支持「全部 / 下载 / 上传」三种视图，并按任务创建时间排序。
- 优化：发布工作流仅接受 main 分支上的 `v*` tag，并自动注入 ChangeLog 内容。
